{
  "name": "lawpowers-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "build:og": "node scripts/build-og.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "astro": "astro",
    "deploy": "pnpm build && firebase deploy --only hosting:lawpowers"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.1.8",
    "powers-landing-shell": "github:crankshift/powers-landing-shell"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "firebase-tools": "^15.15.0",
    "sharp": "^0.34.5",
    "typescript": "~6.0.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
