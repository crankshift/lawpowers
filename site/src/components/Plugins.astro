---
import PluginCard from './PluginCard.astro'
import { UA_AGENTS, PL_AGENTS, UA_SKILLS, PL_SKILLS } from '../data'
import type { Translation } from '../locales/en'

interface Props {
  t: Translation
}

const { t } = Astro.props
---

<section class="section" id="jurisdictions">
  <div class="container">
    <div class="section-head">
      <div>
        <div class="eyebrow">{t.plugins.section_eyebrow}</div>
        <h2>{t.plugins.section_title}</h2>
      </div>
      <p>{t.plugins.section_sub}</p>
    </div>
    <div class="plugin-grid">
      <PluginCard
        code="ua"
        t={t}
        agents={UA_AGENTS}
        skills={UA_SKILLS}
      />
      <PluginCard
        code="pl"
        t={t}
        agents={PL_AGENTS}
        skills={PL_SKILLS}
      />
    </div>
  </div>
</section>

<style>
  .plugin-grid {
    display: grid;
    gap: 24px;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    grid-template-rows: auto auto auto auto auto 1fr;
    align-items: stretch;
  }
  @media (max-width: 960px) {
    .plugin-grid {
      grid-template-columns: minmax(0, 1fr);
      grid-template-rows: auto;
    }
  }
</style>
